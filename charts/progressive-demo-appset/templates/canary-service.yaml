apiVersion: v1
kind: Service
metadata:
  name: podinfo-canary
  namespace: podinfo
  labels:
    app: podinfo

spec:
  selector:
    app: podinfo

  ports:
    - name: http
      port: 9898
      targetPort: 9898